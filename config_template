write your new .config.yaml for the repo. content as followed:

HOME: "/home/chenzhw"
REPO: "Ultrasound-Report-Generation"

organize your repo as followed:

HOME/
  ultrasound_report_gen/
    USData/
    code_repo/
    
then the data folder (data_prefix) would be f'{HOME}/ultrasound_report_gen/USData'
the result folder would be f'{HOME}/ultrasound_report_gen/{REPO}/Result/{self.result}'